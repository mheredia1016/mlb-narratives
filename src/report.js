import {chunkText} from './utils.js';const icons={transaction:'🚑',news:'📰',promotion:'🎁',birthday:'🎂',milestone:'📈'};export function buildMessages(r,c){const l=[`⚾ **MLB Pregame Intel — ${r.date}**`,`Games: ${r.debug.games} | Alerts: ${r.alerts.length}`,''];if(!r.alerts.length)l.push('No qualified pregame intel found.');for(const a of r.alerts){l.push(`${icons[a.type]||'⚾'} **${a.title}**`);if(a.game)l.push(`Game: ${a.game}`);if(a.teamName)l.push(`Team: ${a.teamName}`);l.push(`Score: ${a.score}`);if(a.details)l.push(a.details);if(a.sourceUrl)l.push(a.sourceUrl);l.push('');}l.push(`_Debug: players ${r.debug.activePlayers}, birthdays ${r.debug.birthdays}, milestones ${r.debug.milestones}, transactions ${r.debug.transactions}, promos ${r.debug.promotions}, feeds ${r.debug.newsFeeds}, articles ${r.debug.newsArticles}, fresh ${r.debug.newsFresh}, scored ${r.debug.newsScored}._`);return chunkText(l.join('\n'),1900).slice(0,c.maxDiscordMessages);}export const promoEmbeds=r=>r.alerts.filter(a=>a.type==='promotion'&&a.imageUrl).slice(0,5).map(a=>({title:`🎁 ${a.title}`.slice(0,256),url:a.sourceUrl||undefined,description:[a.game?`**Game:** ${a.game}`:'',a.teamName?`**Team:** ${a.teamName}`:'',a.details?a.details.slice(0,800):''].filter(Boolean).join('\n'),image:{url:a.imageUrl}}));
